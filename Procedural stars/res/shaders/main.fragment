#version 330 core

out vec4 fragColor;

in vec4 v_color;
in vec3 fragPos;
in vec3 normal;

uniform bool u_showNormals;

void main() {
    if (u_showNormals)
        fragColor = vec4(normal, 1);
    else
        fragColor = vec4(v_color);
}