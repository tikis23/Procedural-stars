#version 330 core

layout(location = 0) in vec3 in_position;
layout(location = 1) in vec3 in_color;

out VertexData
{
    vec4 color;
} vertex;

void main()
{
    vertex.color = vec4(in_color.xyz, 1);
    gl_Position = vec4(in_position.xyz, 1);
}