#pragma once

#include <vector>
#include "Shader.h"
#include "QuadTree.h"
#include "glm/glm.hpp"
#include "ThreadPool.h"

enum PLANET_FACE {
	TOP,
	BOTTOM,
	LEFT,
	RIGHT,
	FRONT,
	BACK
};

class Planet {
public:

private:

};